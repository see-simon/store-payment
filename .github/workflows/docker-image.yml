name: Docker Image CI

on:
  [push, pull_request]
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
      - uses: actions/checkout@v4
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag store-payment:latest
      - name: push to Docker Hub
        run:
        

